require 'spec_helper'

describe Allowances do
  it 'has a version number' do
    expect(Allowances::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
